# Respaldo temporal de stores durante la ablación (se regenera en cada corrida)
backend/data/.ablation_snapshot/

# Configuración local de Claude Code
.claude/

# Python
__pycache__/
*.py[cod]

# Variables de entorno locales: cada máquina crea su backend/.env desde
# backend/.env.example. El .env real NO se versiona; la plantilla SÍ.
.env
*.env
!*.env.example
